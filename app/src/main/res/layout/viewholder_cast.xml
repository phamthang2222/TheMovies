<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:minHeight="@dimen/_170sdp"
    android:minWidth="@dimen/_130sdp"
    android:background="@color/prime_color"
    >

    <ImageView
        android:id="@+id/imgCasting"
        android:layout_width="@dimen/_100sdp"
        android:layout_height="@dimen/_100sdp"
        android:layout_marginTop="@dimen/_14sdp"
        android:scaleType="centerCrop"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:srcCompat="@tools:sample/avatars" />

    <TextView
        android:id="@+id/tvCastingName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ho ten"
        android:textAlignment="center"
        android:layout_marginTop="@dimen/_7sdp"
        android:textSize="@dimen/_14sdp"
        android:fontFamily="@font/montserrat_semibold"
        android:textColor="@color/white"
        app:layout_constraintEnd_toEndOf="@+id/imgCasting"
        app:layout_constraintStart_toStartOf="@+id/imgCasting"
        app:layout_constraintTop_toBottomOf="@+id/imgCasting" />

    <TextView
        android:id="@+id/tvCharacter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/montserrat_regular"
        android:text="charactor"
        android:textAlignment="center"
        android:textColor="@color/white"
        android:textSize="@dimen/_11sdp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/tvCastingName"
        app:layout_constraintStart_toStartOf="@+id/tvCastingName"
        app:layout_constraintTop_toBottomOf="@+id/tvCastingName" />
</androidx.constraintlayout.widget.ConstraintLayout>